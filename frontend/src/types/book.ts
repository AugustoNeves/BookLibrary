export interface Book {
  copiesInUse: ReactNode;
  totalCopies: ReactNode;
  firstName: ReactNode;
  lastName: ReactNode;
  title: string;
  publisher: string;
  author: string;
  type: string;
  isbn: string;
  category: string;
  availableCopies: string;
}
